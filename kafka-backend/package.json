{
    "scripts": {
        "client": "cd Frontend && npm start",
        "server": "cd Backend && nodemon",
        "dev": "concurrently \"npm run server\" \"npm run client\""
    },
    "devDependencies": {
        "concurrently": "^6.0.1"
    },
    "dependencies": {
        "kafka-node": "^5.0.0",
        "mongoose": "^6.0.10",
        "multer": "^1.4.3",
        "multer-s3": "^2.10.0",
        "mysql": "^2.18.1"
    }
}
