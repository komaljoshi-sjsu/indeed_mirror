import React, { useEffect } from 'react'
import '../../CSS/Conversation.css'

const Conversation = (props) => {

    return(
        <div className="conversation">
            <span className="conversationName">Akhil</span>
        </div>
    );
}

export default Conversation;
